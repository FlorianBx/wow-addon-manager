{
  "name": "wow-addon-manager",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "build:windows": "CI=false npx tauri build --target x86_64-pc-windows-msvc",
    "build:macos": "CI=false npx tauri build --target aarch64-apple-darwin",
    "build:linux": "CI=false npx tauri build --target x86_64-unknown-linux-gnu",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@vueuse/core": "^14.2.1",
    "vue": "^3.5.25"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.0",
    "@types/node": "^24.10.1",
    "@vitejs/plugin-vue": "^6.0.2",
    "@vue/tsconfig": "^0.8.1",
    "typescript": "~5.9.3",
    "vite": "^7.3.1",
    "vue-tsc": "^3.1.5"
  }
}
